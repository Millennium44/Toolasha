node_modules/
dist/
# Exception: Allow standalone enhancement library for external tools (GreasyFork compatibility)
!dist/libraries/toolasha-enhancement-standalone.js
*.log
.DS_Store
.vscode/
.idea/
.tmp/
docs/*
# Exception: attribution that is not in the repository is not attribution
!docs/THIRD-PARTY-LICENSES.md
# Exception: the GreasyFork listing body carries that attribution to the listing
!docs/GREASYFORK.md
# Exception: a design the maintainer approves section by section belongs in review, not in a scratch file
!docs/overlay-rework-design.md
# Exception: a record of what each claim's verification rests on only works if it outlives the session
!docs/sim-claim-verification.md

# Agent worktrees and session files
.claude/
